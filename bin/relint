#!/usr/bin/env node

var cli,
    exitCode;

/**
 * Module dependencies.
 */

cli = require('../lib/cli');

/**
 * Start the CLI.
 */

exitCode = cli.execute(process.argv);

/**
 * Exit with `exitCode` when drained.
 */

process.on('exit', function() {
    process.exit(exitCode);
});
